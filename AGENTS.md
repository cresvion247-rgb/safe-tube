# AGENTS.md

SafeTube Kids is a Vite + React SPA. Child data is local (Dexie). Parent auth is Supabase. YouTube and learning generation are Vercel functions in `/api`.

Do not reintroduce `@base44/sdk`, `@base44/vite-plugin`, or `base44Client.js`.
