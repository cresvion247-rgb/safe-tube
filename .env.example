# Public Vite env (safe to expose in the browser bundle)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Server-only (Vercel project env — never prefix with VITE_)
YOUTUBE_API_KEY=
OPENAI_API_KEY=
